global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: kcal-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["app:9091"]
